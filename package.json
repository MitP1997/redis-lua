{
  "name": "redis-lua",
  "version": "1.0.0",
  "description": "Executing lua script on remote redis server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "redis": "^4.6.6"
  }
}
